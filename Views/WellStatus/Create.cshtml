@model EDalolatnoma.MVC.Models.WellStatus

@{
    ViewData["Title"] = "Create";
}

<div class="card col-md-6">
    <div class="card-header">
       <h3 class="card-title">
                 
                 Создать новый статус
                </h3> 
    </div>
     <div class="card-body">

        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StatusName" class="control-label">Название</label>
                <input asp-for="StatusName" class="form-control" />
                <span asp-validation-for="StatusName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <a asp-action="Index" class="btn btn-default"><i class="fa fa-solid fa-reply"></i></a>
                <button type="submit" value="" class="btn btn-primary pull-right"><i class="fa fa-solid fa-check"></i> Сохранить</button>
            </div>
        </form>

</div>

    
</div>

   


<div>
  
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
